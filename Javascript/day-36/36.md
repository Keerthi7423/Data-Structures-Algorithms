# DAY 36 — Group Anagrams
**LeetCode #49**  
**Difficulty:** Medium

---

# 0) Problem in Simple Words

Group words that contain the same letters.

### Example

```text
eat
tea
ate

All contain same letters

→ Put them in same group

Real Life Example

Imagine sorting clothes by color:

Red shirt
Red hoodie
Red t-shirt

Same color → same group

Here:

Same letters → same group

Algorithm Used
Brute Force:

Compare every word with every other word

Optimized:

HashMap + Sorted Key Pattern

1) Problem Statement

Input:

["eat","tea","tan","ate","nat","bat"]

Output:

[
 ["eat","tea","ate"],
 ["tan","nat"],
 ["bat"]
]
2) Pattern Recognition

If question says:

group strings
same letters
anagrams

Think:

✅ HashMap
✅ Sorting Pattern

BRUTE FORCE APPROACH
Brute Force Logic

Pick one word

Compare with every remaining word

Check if both are anagrams

Group them together

Brute Force Code
function groupAnagrams(strs) {
    let result = [];
    let visited = new Array(strs.length).fill(false);

    for (let i = 0; i < strs.length; i++) {
        if (visited[i]) continue;

        let group = [strs[i]];
        visited[i] = true;

        for (let j = i + 1; j < strs.length; j++) {
            if (
                strs[i].split('').sort().join('') ===
                strs[j].split('').sort().join('')
            ) {
                group.push(strs[j]);
                visited[j] = true;
            }
        }

        result.push(group);
    }

    return result;
}
Brute Force Line-by-Line Explanation
Line:
let result = [];

Stores final grouped answer

Example:

[
 ["eat","tea","ate"],
 ["tan","nat"]
]
Line:
let visited = new Array(strs.length).fill(false);

Tracks which words are already grouped

Why?

Without this:

same word may get grouped again

Example:

[false,false,false,false]
Line:
for (let i = 0; i < strs.length; i++)

Pick one word

Example:

eat
Line:
if (visited[i]) continue;

Skip already grouped words

Line:
let group = [strs[i]];

Create new group

Example:

["eat"]
Line:
visited[i] = true;

Mark current word as processed

Line:
for (let j = i + 1; j < strs.length; j++)

Compare current word with remaining words

Important Line:
strs[i].split('').sort().join('')

Break word

Sort letters

Join again

Example:

eat → aet
Compare with:
strs[j].split('').sort().join('')

Example:

tea → aet

Same sorted word

→ anagram

Line:
group.push(strs[j]);

Add matching word into group

Example:

["eat","tea"]
Line:
visited[j] = true;

Mark matched word processed

Line:
result.push(group);

Store complete group

Line:
return result;

Return final answer

Brute Force Complexity
Time Complexity
O(n² * k log k)

n = number of words
k = word length

Space Complexity
O(n)
Why Brute Force is Bad
Too many comparisons
Repeated sorting
Slow for large inputs
OPTIMIZED APPROACH
New Algorithm → HashMap Grouping

Use sorted word as key

Example
eat → aet
tea → aet
ate → aet

Same key

→ Same group

Optimized Logic

Loop each word once

Create sorted key

Store inside hashmap

Return grouped values

Optimized Code
function groupAnagrams(strs) {
    let map = {};

    for (let word of strs) {
        let key = word.split('').sort().join('');

        if (!map[key]) {
            map[key] = [];
        }

        map[key].push(word);
    }

    return Object.values(map);
}
Optimized Line-by-Line Explanation
Line:
let map = {};

Stores grouped words

Example:

{
  aet: ["eat","tea"]
}
Line:
for (let word of strs)

Visit every word one by one

Important Line:
let key = word.split('').sort().join('');

Break letters

Sort letters

Join letters

Example:

eat → aet
Line:
if (!map[key])

Check if group exists

Line:
map[key] = [];

Create new group

Line:
map[key].push(word);

Add current word to group

Example:

{
  aet: ["eat","tea","ate"]
}
Line:
return Object.values(map);

Return only grouped arrays

Optimized Complexity
Time Complexity
O(n * k log k)

Much better than brute force

Space Complexity
O(n)
Dry Run

Input:

["eat","tea","ate","bat"]
eat → aet
{
 aet:["eat"]
}
tea → aet
{
 aet:["eat","tea"]
}
ate → aet
{
 aet:["eat","tea","ate"]
}
bat → abt
{
 aet:["eat","tea","ate"],
 abt:["bat"]
}
Visual Representation
eat → aet
tea → aet
ate → aet

↓

Same bucket

tan → ant
nat → ant

↓

Same bucket

Real World Usage
Google
search clustering
text grouping
JPMorgan Chase
transaction grouping
fraud grouping
Common Mistakes
Forgetting sorted key
Returning hashmap directly
Wrong grouping logic
Revision Notes

Problem:

Group words with same letters

Real Life:

Same color clothes grouped together

Brute:

Compare everything
O(n²)

Optimized:

Sort word → use hashmap
O(n * k log k)
5 Second Recall
Same sorted letters = same group
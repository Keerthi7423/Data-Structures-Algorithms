========================================
DAY-11 STRINGS PRACTICE
ANSWERS + DETAILED EXPLANATION
========================================

Score: 9.5 / 10
Level: Solid Intermediate

----------------------------------------
1) CLEAN + UPPERCASE
----------------------------------------
let text = "  hello world  ";

let clean = text.trim();        // remove spaces
let upper = clean.toUpperCase(); // convert to uppercase

console.log(upper);

Output: HELLO WORLD

----------------------------------------
2) EXTRACT FIRST 5 CHARACTERS
----------------------------------------
let str = "JavaScript";

let result = str.slice(0, 5);

console.log(result);

Output: JavaS

----------------------------------------
3) REPLACE WORD
----------------------------------------
let msg = "Hello World";

let result = msg.replace("World", "Keerthi");

console.log(result);

Output: Hello Keerthi

----------------------------------------
4) CHECK SUBSTRING
----------------------------------------
let str = "JavaScript";

let result = str.includes("Script");

console.log(result);

Output: true

----------------------------------------
5) CLEAN + UPPERCASE (BEST ORDER)
----------------------------------------
let name = "  keerthi  ";

let clean = name.trim();          // clean first
let upper = clean.toUpperCase();  // then convert

console.log(upper);

Output: KEERTHI

----------------------------------------
6) EXTRACT + LOWERCASE
----------------------------------------
let str = "JavaScript";

let part = str.slice(4);          // "Script"
let lower = part.toLowerCase();   // "script"

console.log(lower);

Output: script

----------------------------------------
7) CLEAN → UPPERCASE
----------------------------------------
let text = "  hello js  ";

let result = text.trim().toUpperCase();

console.log(result);

Output: HELLO JS

----------------------------------------
8) REPLACE + UPPERCASE
----------------------------------------
let str = "hello world";

let result = str.replace("world", "JS").toUpperCase();

console.log(result);

Output: HELLO JS

----------------------------------------
9) COMPLEX LOGIC
----------------------------------------
let name = "  keerthi kumar  ";

let clean = name.trim();          // "keerthi kumar"
let upper = clean.toUpperCase();  // "KEERTHI KUMAR"
let first = upper.slice(0, 7);    // "KEERTHI"

console.log(`Hello ${first}`);

Output: Hello KEERTHI

----------------------------------------
10) CAPITALIZE FIRST LETTER
----------------------------------------
let str = "javascript";

let first = str[0].toUpperCase();
let rest = str.slice(1);

let result = first + rest;

console.log(result);

Output: Javascript

----------------------------------------
11) CLEAN + SLICE + UPPERCASE
----------------------------------------
let str = "  web development  ";

let result = str.trim().slice(0, 3).toUpperCase();

console.log(result);

Output: WEB

----------------------------------------
BONUS OUTPUTS
----------------------------------------

12) "  hello  "
trim → "hello"
uppercase → "HELLO"
slice(0,3) → "HEL"

Answer: HEL

----------------------------------------

13) "JavaScript"
slice(-6) → "Script"
uppercase → "SCRIPT"

Answer: SCRIPT

----------------------------------------

14) "  js  "
trim → "js"
uppercase → "JS"

Answer: JS

----------------------------------------
FINAL SUMMARY
----------------------------------------

✔ Always:
trim() → clean input
then → transform (uppercase/lowercase)
then → extract (slice)

✔ Method chaining = powerful
✔ Think step-by-step

----------------------------------------
END
----------------------------------------
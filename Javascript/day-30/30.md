# 🚀 Day 30 — Mini Project (CLI Calculator)

---

## 📘 Goal of Day 30

Apply everything learned in Month 1:

* Variables
* Operators
* Conditions
* Functions
* ES6

👉 Build a **real working program**

---

## 🧠 What is a CLI Calculator?

CLI = Command Line Interface

A calculator that:

* Takes input
* Performs operation
* Shows result

---

## ⚡ Core Concept

**Input → Operation → Output**

---

## 💻 Full Project Code

```javascript
// Day 30 - CLI Calculator

function calculator(a, b, operator) {
  if (operator === "+") {
    return a + b;
  } else if (operator === "-") {
    return a - b;
  } else if (operator === "*") {
    return a * b;
  } else if (operator === "/") {
    return b !== 0 ? a / b : "Cannot divide by zero";
  } else {
    return "Invalid operator";
  }
}

// Test cases
console.log(calculator(10, 5, "+"));
console.log(calculator(10, 5, "-"));
console.log(calculator(10, 5, "*"));
console.log(calculator(10, 5, "/"));
```

---

## 🔍 How It Works

* Inputs:

  * `a` → number
  * `b` → number
  * `operator` → `+ - * /`

* Uses `if/else` to decide operation

* Returns result

---

## ⚡ Improved Version (ES6 Style)

```javascript
const calculator = (a, b, op) => {
  switch (op) {
    case "+":
      return a + b;
    case "-":
      return a - b;
    case "*":
      return a * b;
    case "/":
      return b !== 0 ? a / b : "Error";
    default:
      return "Invalid";
  }
};

console.log(calculator(20, 10, "*"));
```

---

## 🧠 Concepts Used

* Functions
* Parameters & return
* if / else
* switch
* Operators
* ES6 (arrow functions)

---

## 📊 Month 1 Completed 🎉

You covered:

* JavaScript basics
* Strings
* Conditions
* Loops
* Arrays
* Objects
* Functions
* ES6

👉 Strong foundation built ✅

---

## 🚀 What Next?

Month 2 Focus:

* Logic problems
* DOM
* Real applications

👉 Start:

* LeetCode (Day 31+)

---

## 📌 Summary

* Built CLI Calculator
* Applied all core concepts
* Completed Month 1
* Ready for next level

---

## ✅ Status

✔ Day 30 Completed
✔ Month 1 Completed
✔ Ready for advanced learning

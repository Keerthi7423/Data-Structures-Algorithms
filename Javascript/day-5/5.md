========================================
DAY-3 JAVASCRIPT INTERVIEW ANSWERS
TOPIC: OPERATORS
========================================

----------------------------------------
1) WHAT ARE OPERATORS?
----------------------------------------
Operators are symbols used to perform operations on values and variables.

Example:
let x = 10 + 5;

----------------------------------------
2) TYPES OF OPERATORS
----------------------------------------
- Arithmetic Operators
- Assignment Operators
- Comparison Operators
- Logical Operators
- Type Operators

----------------------------------------
3) DIFFERENCE BETWEEN == AND ===
----------------------------------------
==  → compares only values
=== → compares both value and data type

Example:
5 == "5"  → true
5 === "5" → false

----------------------------------------
4) MODULUS (%) OPERATOR
----------------------------------------
The modulus operator returns the remainder of a division.

Example:
10 % 3 → 1

----------------------------------------
5) DIFFERENCE BETWEEN x++ AND ++x
----------------------------------------

x++ (Post-increment):
- Returns value first
- Then increments

Example:
let x = 5;
console.log(x++); → 5
console.log(x);   → 6

----------------------------------------

++x (Pre-increment):
- Increments first
- Then returns value

Example:
let x = 5;
console.log(++x); → 6

----------------------------------------
6) LOGICAL OPERATORS
----------------------------------------
Logical operators combine conditions and return boolean values.

Operators:
&& → AND (both true)
|| → OR (one true)
!  → NOT (reverse)

Example:
true && false → false

----------------------------------------
7) TYPEOF OPERATOR
----------------------------------------
typeof is used to check the data type of a value.

Examples:
typeof "Hello" → string
typeof 123 → number
typeof true → boolean

----------------------------------------
8) console.log(5 + "5")
----------------------------------------
Output: "55"

Explanation:
Number is converted to string → concatenation happens.

----------------------------------------
9) console.log(5 - "2")
----------------------------------------
Output: 3

Explanation:
String is converted to number → subtraction happens.

----------------------------------------
10) console.log("10" * 2)
----------------------------------------
Output: 20

Explanation:
String converted to number → multiplication.

----------------------------------------
11) console.log(10 % 3)
----------------------------------------
Output: 1

Explanation:
Remainder of 10 divided by 3.

----------------------------------------
12) let x = 5; console.log(x++);
----------------------------------------
Output: 5

Explanation:
Post-increment → returns value first, then increments.

----------------------------------------
13) let x = 5; console.log(++x);
----------------------------------------
Output: 6

Explanation:
Pre-increment → increments first.

----------------------------------------
14) console.log(true && false)
----------------------------------------
Output: false

Explanation:
AND requires both true.

----------------------------------------
15) console.log(true || false)
----------------------------------------
Output: true

Explanation:
OR requires at least one true.

----------------------------------------
16) console.log(!true)
----------------------------------------
Output: false

Explanation:
NOT reverses boolean value.

----------------------------------------
17) console.log(5 == "5")
----------------------------------------
Output: true

Explanation:
Value is same → type ignored.

----------------------------------------
18) console.log(5 === "5")
----------------------------------------
Output: false

Explanation:
Different data types.

----------------------------------------
19) EVEN OR ODD PROGRAM
----------------------------------------
let num = 7;

if (num % 2 === 0) {
    console.log("Even");
} else {
    console.log("Odd");
}

----------------------------------------
20) GREATER THAN 10 PROGRAM
----------------------------------------
let num = 12;

if (num > 10) {
    console.log("Greater than 10");
} else {
    console.log("10 or less");
}

----------------------------------------
21) RANGE CHECK (1 TO 100)
----------------------------------------
let num = 50;

if (num >= 1 && num <= 100) {
    console.log("In range");
} else {
    console.log("Out of range");
}

----------------------------------------
22) SWAP USING THIRD VARIABLE
----------------------------------------
let a = 5, b = 10, temp;

temp = a;
a = b;
b = temp;

----------------------------------------
23) SWAP WITHOUT THIRD VARIABLE
----------------------------------------
let a = 10, b = 20;

a = a + b;
b = a - b;
a = a - b;

----------------------------------------
24) console.log("5" + 2 + 3)
----------------------------------------
Output: "523"

Explanation:
String first → all become strings → concatenation.

----------------------------------------
25) console.log(2 + 3 + "5")
----------------------------------------
Output: "55"

Explanation:
2+3=5 → then string → concatenation.

----------------------------------------
26) console.log(false == 0)
----------------------------------------
Output: true

Explanation:
false is converted to 0.

----------------------------------------
27) console.log(false === 0)
----------------------------------------
Output: false

Explanation:
Different types → strict comparison fails.

----------------------------------------
END
----------------------------------------